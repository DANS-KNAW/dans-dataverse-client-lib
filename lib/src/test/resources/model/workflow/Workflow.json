{
  "id": 12,
  "name": "Ingest Workflow",
  "steps": [
    {
      "stepType": "api",
      "provider": "http://example.com/workflow/step",
      "parameters": {
        "method": "POST",
        "datasetId": ":persistentId"
      },
      "requiredSettings": {
        "authToken": "auth.example.token"
      }
    }
  ]
}
